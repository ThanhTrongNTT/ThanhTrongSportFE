const ErrorBoundary = () => {
    return (
        <div className="rounded-lg bg-red-100 p-5 text-red-500">
            Look like component is error
        </div>
    );
};

export default ErrorBoundary;
